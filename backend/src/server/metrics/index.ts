// Metrics aggregation index (T043)
// Currently exposes tickMetrics; future metrics (latency harness, room load) will register here.
export { tickMetrics } from "./tickMetrics.ts";

// Future placeholder:
// export { inputLatencyMetrics } from './inputLatency.ts';